[project]
name = "router-rebooter"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "selenium==4.41.0",
    "webdriver-manager==4.0.2",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.4",
    "pytest-mock=3.14.0"
]
